# Zotero 自动抓取论文操作指南（271 篇近岸养殖政策文献）

> 目标：让 Zotero 自动抓取 271 篇文献的**元数据**与**可获取的全文 PDF**。
> 配套文件：`zotero_import_271.ris`（已含全部条目与 138 个 DOI）、`zotero_doi_list.txt`（纯 DOI 清单，供批量添加）。

---

## 一、准备（一次即可）

1. 打开 Zotero 桌面版，新建分类（如「近岸养殖政策 271」）
2. `文件 → 导入 → 选择 zotero_import_271.ris` → 导入到该分类
   - 此时 271 条已入库；**138 条含 DOI 的条目**可继续自动增强

---

## 二、自动抓取全文 PDF（推荐路径，全自动）

**前提**：Zotero 7（或 6.0.27+），联网，尽量配置代理/校园网以便访问 ScienceDirect 等。

1. 在分类中 **Ctrl+A 全选**（或按地区/主题筛选后选择）
2. 右键 → **查找可用的 PDF（Find Available PDF）**
   - Zotero 会逐个查询 Unpaywall / PubMed Central / 期刊开放页面
   - **开放获取（OA）的论文自动下载 PDF 并挂到条目**，同时用 PDF 内嵌元数据补全条目（自动抓取）
3. 对未抓到 PDF 的条目：右键 → **查找元数据（Retrieve Metadata）**（有 DOI/标题时自动抓取补齐作者/卷期/页码）

> 效果：OA 文献（如 CN-55、US-14/15/17、JP-18、NO-04、CL-01/04、EU-34、EU-01/11/21 等标注 OA 者）基本能自动拿到全文；付费墙文献仅补全元数据。

---

## 三、通过 DOI 批量添加（替代/补充路径）

若希望**重建一套干净条目**（而非基于 RIS），Zotero 支持一次粘贴多个 DOI：

1. 点击工具栏绿色加号 **「通过标识符添加（Add Item(s) by Identifier）」**（一个放大镜+加号图标）
2. 把 `zotero_doi_list.txt` 的内容**整段粘贴**进去（每行一个 DOI）
3. 回车/确认 → Zotero 自动按 DOI 逐条抓取元数据并入库
   - 抓取来源：Crossref / DataCite / 出版社
4. 全选后重复"第二部分第 2 步"的 **查找可用的 PDF**，批量补 OA 全文

> 说明：此路径适用于 DOI 完整、希望条目字段完全由出版社源补全的场景；中文/日文期刊（CNKI/J-STAGE，多数无 DOI）不在此列，需用第 4/5 部分处理。

---

## 四、Zotero Connector 浏览器插件（逐篇补充用）

对抓取失败的条目（尤其 CNKI/J-STAGE/期刊官网页面）：

1. 安装浏览器扩展 **Zotero Connector**，并在 Zotero 设置中勾选对应站点（CNKI 需手动添加站点规则）
2. 打开论文页面（如知网详情页、J-STAGE 文章页、期刊官网 PDF 页）
3. 点浏览器工具栏 Zotero 图标 → 自动抓取该页元数据与 PDF 存入对应分类

---

## 五、自动抓取元数据设置（常开项）

`编辑 → 设置 → 常规`：
- ☑ **自动抓取 PDF 中的元数据（Automatically retrieve metadata for PDFs）**——拖入 PDF 时自动识别标题/作者/DOI
- ☑ **PDF 索引**（全文检索用，可选）

`编辑 → 设置 → 高级`：可调整 **Retrieval** 相关代理（校园网/Elsevier 直连时可能需要）

---

## 六、无 DOI 条目的处理（约 132 条）

这些多为中文/日文期刊（CNKI/J-STAGE）或"DOI 未核实"项，Zotero 无法自动抓取，两种方式：

| 方式 | 操作 | 适用 |
|---|---|---|
| 手动补全 | 条目已含标题/期刊/卷期 → 右键"查找元数据"或手工补作者/页码 | 任何条目 |
| 半自动 | 用 Connector 打开 CNKI/J-STAGE 页面抓取 | 期刊官网可访问时 |

> 提示：总目中标注 `※作者待核实` 的 98 条，作者栏为空，需按此方式补全；可参照《近岸养殖政策文献总览阅读报告》第五节核对。

---

## 七、抓取失败排查

- **ScienceDirect/Springer 抓不到**：多为访问权限问题 → 启用校园网/VPN 代理（Zotero 设置→高级→连接）
- **Unpaywall 未命中**：该文确非 OA → 只补元数据，PDF 需馆藏/文献传递
- **中文期刊**：CNKI 需在 Connector 中配置站点翻译器（Zotero 翻译器仓库更新后支持）
- **批量量大**：271 条"查找可用的 PDF"会请求大量接口，建议按地区/主题分批次（每批 30~50 条）执行，避免被限流

---

## 八、预期效果

| 条目类型 | 数量 | 自动抓取效果 |
|---|---|---|
| 含 DOI 且 OA | 约 40+ | 元数据 + PDF 全自动 |
| 含 DOI 付费 | 约 95 | 元数据自动，PDF 需手动 |
| 无 DOI（中/日文等） | 约 132 | 需 Connector/手动补全 |

> 与文献库配合：《每地区精读文献精选清单_65篇.md》中的精读对象优先抓取；《近岸养殖政策文献总览阅读报告》可作阅读导航。
